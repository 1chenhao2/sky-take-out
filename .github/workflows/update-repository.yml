name: Update repository

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - shell: bash
        run: |
          unzip -o project-source.zip
          rm -f project-source.zip sky-take-out.bundle
          git config user.name "1chenhao2"
          git config user.email "1357447550@qq.com"
          git add -A
          git commit -m "feat: publish sky takeout platform"
          git push origin HEAD:main
